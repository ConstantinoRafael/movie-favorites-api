# Ambiente
NODE_ENV=development
PORT=3000

# Banco de dados (exemplo — configurar quando @nestjs/typeorm ou Prisma for adicionado)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Autenticação (exemplo — configurar quando @nestjs/jwt for adicionado)
# JWT_SECRET=
# JWT_EXPIRES_IN=3600s
